# Как использовать систему исследования через бота Crafto

## 🎯 Быстрый старт

### 1. Создание бота
```
/crafto spawn MyExplorer
```

### 2. Команды через AI (естественный язык)
```
/crafto tell MyExplorer исследуй область
/crafto tell MyExplorer найди алмазы
/crafto tell MyExplorer создай точку дом
/crafto tell MyExplorer иди домой
/crafto tell MyExplorer создай карту
```

### 3. Прямые команды (для точного контроля)
```
/crafto explore MyExplorer 128
/crafto waypoint create MyExplorer home BASE
/crafto find MyExplorer diamond_ore
/crafto map create MyExplorer 200
/crafto stats MyExplorer
```

## 🤖 AI Команды (естественный язык)

Бот понимает команды на русском и английском языках:

### Исследование
- `"исследуй область"` - исследует 64 блока вокруг
- `"explore area"` - исследует область по умолчанию
- `"исследуй 200 блоков"` - исследует указанный радиус

### Поиск ресурсов
- `"найди алмазы"` - найдет ближайшие алмазы
- `"find diamonds"` - найдет алмазную руду
- `"найди железо"` - найдет железную руду
- `"find gold"` - найдет золото

### Путевые точки
- `"создай точку дом"` - создаст путевую точку "дом" типа BASE
- `"create waypoint mine"` - создаст точку "mine" типа MINE
- `"иди домой"` - навигация к точке "домой"
- `"go to mine"` - навигация к точке "mine"

### Карты
- `"создай карту"` - создаст карту области 200 блоков
- `"create map"` - создаст карту по умолчанию
- `"покажи карту"` - экспортирует текущую карту

## 📋 Прямые команды (точный контроль)

### Исследование
```bash
/crafto explore <имя_бота> <радиус>
# Пример: /crafto explore MyBot 128
```

### Путевые точки
```bash
# Создание
/crafto waypoint create <имя_бота> <имя_точки> <тип>
# Пример: /crafto waypoint create MyBot home BASE

# Навигация
/crafto waypoint navigate <имя_бота> <имя_точки>
# Пример: /crafto waypoint navigate MyBot home
```

**Типы путевых точек:**
- `BASE` - база, дом
- `MINE` - шахта, месторождение
- `FARM` - ферма
- `LANDMARK` - ориентир
- `TRADING_POST` - торговый пост
- `DANGER_ZONE` - опасная зона

### Поиск ресурсов
```bash
/crafto find <имя_бота> <тип_ресурса>
# Примеры:
/crafto find MyBot diamond_ore
/crafto find MyBot iron_ore
/crafto find MyBot gold_ore
/crafto find MyBot coal_ore
```

### Карты
```bash
/crafto map create <имя_бота> <радиус>
# Пример: /crafto map create MyBot 300
```

### Статистика
```bash
/crafto stats <имя_бота>
# Показывает статистику исследования и навигации
```

## 🎮 Практические примеры

### Сценарий 1: Первое исследование
```bash
# 1. Создаем бота
/crafto spawn Explorer

# 2. Создаем домашнюю точку
/crafto tell Explorer создай точку дом

# 3. Исследуем область
/crafto tell Explorer исследуй область радиусом 200 блоков

# 4. Смотрим что нашли
/crafto stats Explorer
```

### Сценарий 2: Поиск ресурсов
```bash
# 1. Ищем алмазы
/crafto tell Explorer найди алмазы

# 2. Если не нашли, исследуем больше
/crafto tell Explorer исследуй область радиусом 500 блоков

# 3. Ищем снова
/crafto find Explorer diamond_ore
```

### Сценарий 3: Создание навигационной сети
```bash
# 1. Создаем точки в разных местах
/crafto waypoint create Explorer spawn BASE
/crafto waypoint create Explorer mine MINE
/crafto waypoint create Explorer farm FARM

# 2. Навигируемся между ними
/crafto tell Explorer иди к шахте
/crafto tell Explorer иди на ферму
/crafto tell Explorer иди домой
```

### Сценарий 4: Картографирование
```bash
# 1. Исследуем большую область
/crafto tell Explorer исследуй область радиусом 1000 блоков

# 2. Создаем карту
/crafto map create Explorer 1000

# 3. Смотрим статистику
/crafto stats Explorer
```

## 🔧 Автоматические функции

Бот автоматически:

✅ **Создает путевые точки** для найденных ценных ресурсов  
✅ **Обновляет карты** при исследовании новых областей  
✅ **Сохраняет данные** в JSON файлы  
✅ **Находит оптимальные пути** между точками  
✅ **Избегает опасных зон** при навигации  

## 📊 Мониторинг

### Просмотр статистики
```bash
/crafto stats MyBot
```

Показывает:
- Количество исследованных областей
- Найденные ресурсы по типам
- Созданные путевые точки
- Построенные дороги
- Качество навигационной сети

### Файлы данных
Система сохраняет данные в:
```
config/crafto/
├── exploration/
│   ├── explored_areas.json
│   ├── resource_locations.json
│   └── danger_zones.json
├── waypoints/
│   ├── waypoints.json
│   └── roads.json
└── maps/
    ├── world_map.json
    └── shared_maps.json
```

## 🚀 Продвинутые возможности

### Комбинированные команды
```bash
# Исследовать и создать карту
/crafto tell MyBot исследуй область и создай карту

# Найти ресурс и создать точку
/crafto tell MyBot найди железо и создай точку шахта
```

### Интеграция с другими системами
Система исследования интегрируется с:
- **Долгосрочной памятью** - запоминает успешные стратегии
- **Планировщиком задач** - автоматически планирует исследования
- **Системой строительства** - использует найденные ресурсы

## ❗ Важные заметки

1. **Производительность**: Исследование больших областей может занять время
2. **Память**: Система сохраняет все данные автоматически
3. **Безопасность**: Бот избегает опасных зон при навигации
4. **Совместимость**: Работает с существующими командами Crafto

## 🆘 Решение проблем

### Бот не отвечает на команды исследования
```bash
# Проверьте, что бот существует
/crafto list

# Перезапустите бота
/crafto remove MyBot
/crafto spawn MyBot
```

### Не находит ресурсы
```bash
# Сначала исследуйте область
/crafto tell MyBot исследуй область радиусом 300 блоков

# Затем ищите ресурсы
/crafto find MyBot diamond_ore
```

### Ошибки навигации
```bash
# Проверьте существование путевой точки
/crafto stats MyBot

# Создайте точку заново если нужно
/crafto waypoint create MyBot home BASE
```

Система готова к использованию! Начните с простых команд и постепенно изучайте все возможности.