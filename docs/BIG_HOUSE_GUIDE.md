# 🏠 Руководство по строительству большого дома

## Описание

Система строительства большого дома позволяет боту Steve (@Alex) строить детализированные дома по заранее подготовленным шаблонам NBT.

## Возможности

### Большой дом (Big House)
- **Размеры**: 15x8x12 блоков
- **Этажность**: 2 этажа
- **Комнаты**: 
  - Гостиная с мебелью
  - Кухня с печью и сундуками
  - Спальня на втором этаже
  - Ванная комната
- **Особенности**:
  - Многоуровневая крыша
  - Множество окон
  - Декоративные колонны
  - Внутренняя лестница
  - Полностью меблированные комнаты

## Команды

### На английском:
```
@Alex build big house
```

### На русском:
```
@Alex построй большой дом
```

## Как это работает

1. **Загрузка шаблона**: При запуске мода автоматически генерируется NBT файл `structures/big-house.nbt` с планом дома
2. **Обработка команды**: Когда вы даете команду боту, AI распознает запрос и создает задачу строительства
3. **Строительство**: Бот загружает NBT шаблон и строит дом блок за блоком
4. **Коллаборация**: Несколько ботов могут работать вместе над одним домом

## Добавление собственных структур

### Шаг 1: Создание NBT файла

Вы можете создать свой NBT файл двумя способами:

#### Способ 1: Конвертация из Schematic
```java
// Поместите .schematic файл в папку structures/
// При запуске мода он автоматически конвертируется в NBT
```

#### Способ 2: Программная генерация
```java
// Создайте класс-генератор по примеру BigHouseGenerator.java
// Добавьте вызов в CraftoMod.commonSetup()
```

### Шаг 2: Добавление в систему команд

1. Откройте `PromptBuilder.java`
2. Добавьте вашу структуру в список `SUPPORTED STRUCTURES`:
```java
- my-structure/моя-структура: Description of your structure
```

3. Добавьте примеры команд:
```java
"build my structure" -> {"reasoning": "...", "plan": "...", "tasks": [{"action": "build", "parameters": {"structure": "my-structure"}}]}
```

### Шаг 3: Тестирование

1. Запустите Minecraft с модом
2. Создайте бота: `/crafto spawn Alex`
3. Дайте команду: `@Alex build my structure`

## Формат NBT файла

NBT файл структуры должен содержать:

```nbt
{
  "size": [width, height, depth],
  "palette": [
    {"Name": "minecraft:block_name"},
    ...
  ],
  "blocks": [
    {
      "state": palette_index,
      "pos": [x, y, z]
    },
    ...
  ]
}
```

## Материалы

Бот автоматически получает необходимые материалы из инвентаря. Система автоматически пополняет инвентарь при необходимости.

## Производительность

- **Скорость строительства**: 1 блок за тик (20 блоков/секунду)
- **Режим полета**: Автоматически включается для быстрого строительства
- **Коллаборация**: Несколько ботов могут строить одновременно

## Отладка

Если дом не строится:

1. Проверьте логи: `logs/latest.log`
2. Убедитесь, что NBT файл существует: `structures/big-house.nbt`
3. Проверьте доступные структуры в логах при запуске
4. Используйте команду `/crafto tell Alex show inventory` для проверки инвентаря

## Примеры использования

### Базовое строительство
```
@Alex build big house
```

### Строительство несколькими ботами
```
/crafto spawn Builder1
/crafto spawn Builder2
@Builder1 build big house
@Builder2 build big house
```
(Оба бота будут работать над одним домом)

### Остановка строительства
```
/crafto stop Alex
```

## Технические детали

- **Файл генератора**: `src/main/java/com/crafto/ai/structure/BigHouseGenerator.java`
- **Загрузчик шаблонов**: `src/main/java/com/crafto/ai/structure/StructureTemplateLoader.java`
- **Действие строительства**: `src/main/java/com/crafto/ai/action/actions/BuildStructureAction.java`
- **Конвертер schematic**: `src/main/java/com/crafto/ai/structure/SchematicConverter.java`

## Известные ограничения

1. NBT файл должен быть в формате Minecraft Structure
2. Максимальный размер структуры ограничен памятью
3. Сложные блоки (двери, кровати) могут требовать дополнительной настройки
4. Некоторые блоки из старых версий могут не конвертироваться корректно

## Будущие улучшения

- [ ] Поддержка блоков с данными (направление, состояние)
- [ ] Импорт структур из WorldEdit
- [ ] Библиотека готовых домов
- [ ] Редактор структур в игре
- [ ] Случайная генерация вариаций домов
- [ ] Адаптация к ландшафту

## Поддержка

Если у вас возникли проблемы:
1. Проверьте логи
2. Убедитесь, что используете последнюю версию мода
3. Создайте issue на GitHub с описанием проблемы и логами
