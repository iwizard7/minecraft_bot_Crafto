# 🚀 Ollama Setup для MacBook M2

## Быстрая установка

### 1. Установи Ollama
```bash
# Скачай с https://ollama.ai или через Homebrew:
brew install ollama

# Запусти сервер
ollama serve
```

### 2. Установи оптимальные модели для M2

#### Для максимальной скорости (рекомендуется):
```bash
ollama pull qwen2.5:7b    # Самая быстрая, отлично для кода
```

#### Для экономии памяти:
```bash
ollama pull llama3.2:3b   # Маленькая, но быстрая
```

#### Универсальная:
```bash
ollama pull mistral:7b    # Сбалансированная
```

### 3. Проверь работу
```bash
ollama run qwen2.5:7b "Hello, how are you?"
```

## 🎯 Оптимальные настройки для M2

### В steve-common.toml:
```toml
[ai]
provider = "local"

[ollama]
model = "qwen2.5:7b"  # Или llama3.2:3b для экономии
maxTokens = 400
temperature = 0.7

[behavior]
maxActiveSteves = 2   # Не больше для локального AI
actionTickDelay = 15  # Дай время на обработку
```

## 📊 Производительность моделей на M2

| Модель | Размер | Скорость | Память | Качество |
|--------|--------|----------|---------|----------|
| llama3.2:3b | 2GB | ⚡⚡⚡ | 4GB | ⭐⭐⭐ |
| qwen2.5:7b | 4GB | ⚡⚡ | 6GB | ⭐⭐⭐⭐ |
| mistral:7b | 4GB | ⚡⚡ | 6GB | ⭐⭐⭐⭐ |

## 🔧 Troubleshooting

### Ollama не запускается:
```bash
# Проверь порт
lsof -i :11434

# Перезапусти
pkill ollama
ollama serve
```

### Слишком медленно:
1. Используй `llama3.2:3b` вместо 7b модели
2. Уменьши `maxActiveSteves` до 1
3. Закрой Chrome и другие тяжёлые приложения
4. Увеличь `actionTickDelay` до 20-30

### Не хватает памяти:
```bash
# Проверь использование памяти
ollama ps

# Выгрузи неиспользуемые модели
ollama rm model_name
```

## ⚡ Экстремальная оптимизация

Если всё ещё тормозит, попробуй:

1. **Самую быструю модель:**
```bash
ollama pull tinyllama:1.1b  # Очень маленькая, но слабая
```

2. **Кастомные параметры в коде:**
- `num_thread`: 6-8 (количество ядер M2)
- `num_ctx`: 2048 (контекст)
- `temperature`: 0.5 (меньше креативности = быстрее)

3. **Мониторинг:**
```bash
# Следи за нагрузкой
top -pid $(pgrep ollama)
```

## 🎮 Готово к игре!

После настройки:
1. Запусти `ollama serve`
2. Скопируй `steve-common-optimized.toml` в `steve-common.toml`
3. Запускай Minecraft и наслаждайся быстрым локальным AI!