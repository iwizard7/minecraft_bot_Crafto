# Agent Framework Dependencies
# These are conceptual equivalents - actual implementation is in Java

# Core LangChain-style components
langchain>=0.1.0
langchain-community>=0.0.20

# Vector store and embeddings
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Local LLM provider only
# ollama - no Python package needed, uses HTTP API

# Memory and storage
redis>=5.0.0
faiss-cpu>=1.7.0

# ReAct framework
react-agent>=1.0.0

# Utilities
tiktoken>=0.5.0
numpy>=1.24.0
pydantic>=2.0.0

# Note: This project uses Java implementations inspired by these frameworks
# No Python dependencies are actually required to run the mod

