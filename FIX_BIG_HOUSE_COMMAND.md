# 🔧 Исправление команды "build big house"

## Проблема
Команда "build big house" строила старый процедурный дом вместо детализированного дома из NBT шаблона.

## Исправления

### 1. Нормализация имени структуры
**Файл:** `BuildStructureAction.java`

Добавлена автоматическая замена пробелов на дефисы:
```java
// "big house" -> "big-house"
structureType = structureType.replace(" ", "-");
```

### 2. Улучшенные промпты для AI
**Файл:** `PromptBuilder.java`

Добавлены:
- Явное указание разницы между "house" и "big-house"
- Дополнительные примеры команд
- Предупреждение для AI: "big house" = "big-house" (NOT "house"!)

### 3. Дополнительные примеры
Добавлены варианты команд:
- "build big house"
- "build a big house"
- "build large house"
- "построй большой дом"
- "построй огромный дом"

## Как тестировать

### 1. Удалите старый NBT файл (если есть)
```bash
rm structures/big-house.nbt
```

### 2. Соберите проект
```bash
./gradlew build -x test
```

### 3. Запустите Minecraft
NBT файл будет автоматически сгенерирован при запуске мода.

### 4. Проверьте логи
Должны быть сообщения:
```
[INFO] Big house NBT not found, generating...
[INFO] Generated big house NBT: ~800-1000 blocks, 15x8x12 at structures/big-house.nbt
```

### 5. В игре протестируйте команды

#### На английском:
```
/crafto spawn Alex
@Alex build big house
```

#### На русском:
```
/crafto spawn Алекс
@Алекс построй большой дом
```

### 6. Проверьте логи строительства
Должны быть сообщения:
```
[INFO] Building structure: 'big-house'
[INFO] Loaded 'big-house' from NBT template with ~800-1000 blocks
[INFO] Crafto 'Alex' PLACED BLOCK at ...
```

## Что должно произойти

### ✅ Правильное поведение:
1. Бот начинает летать
2. Строится дом 15×8×12 блоков
3. Дом имеет:
   - 2 этажа
   - Лестницу между этажами
   - Гостиную с мебелью
   - Кухню с печью
   - Спальню с кроватью
   - Множество окон
   - Декоративные колонны

### ❌ Неправильное поведение (старая версия):
- Строится простой процедурный дом
- Нет мебели
- Нет второго этажа
- Простая плоская крыша

## Отладка

### Если строится старый дом:

1. **Проверьте логи AI:**
   ```bash
   grep "structure" logs/latest.log
   ```
   Должно быть: `"structure": "big-house"`
   НЕ должно быть: `"structure": "house"`

2. **Проверьте NBT файл:**
   ```bash
   ls -lh structures/big-house.nbt
   ```
   Размер должен быть ~2-5 KB, НЕ 138 байт

3. **Проверьте загрузку:**
   ```bash
   grep "Loaded.*from NBT" logs/latest.log
   ```
   Должно быть: "Loaded 'big-house' from NBT template"

4. **Пересоздайте NBT файл:**
   ```bash
   rm structures/big-house.nbt
   # Перезапустите Minecraft
   ```

### Если AI возвращает неправильную структуру:

Проверьте ответ AI в логах:
```bash
grep -A 5 "ResponseParser" logs/latest.log
```

Если AI возвращает `"structure": "house"` вместо `"structure": "big-house"`:
- Убедитесь что используете обновленный PromptBuilder.java
- Попробуйте более явную команду: "build a big house from template"

## Технические детали

### Поток обработки команды:

```
"build big house"
    ↓
AI: {"structure": "big-house"}  или  {"structure": "big house"}
    ↓
BuildStructureAction: normalize "big house" -> "big-house"
    ↓
tryLoadFromTemplate("big-house")
    ↓
StructureTemplateLoader.loadFromNBT("big-house")
    ↓
Поиск: structures/big-house.nbt
    ↓
Загрузка NBT и строительство
```

### Файлы изменены:
- `src/main/java/com/crafto/ai/action/actions/BuildStructureAction.java`
- `src/main/java/com/crafto/ai/ai/PromptBuilder.java`

### Размер изменений:
- +10 строк кода
- +5 примеров команд
- +1 нормализация имени

## Коммит

```bash
git add src/main/java/com/crafto/ai/action/actions/BuildStructureAction.java
git add src/main/java/com/crafto/ai/ai/PromptBuilder.java
git commit -m "fix: Normalize structure names and improve big-house command recognition

- Add automatic space-to-dash conversion in BuildStructureAction
- Improve AI prompts to distinguish between 'house' and 'big-house'
- Add more command examples for big house
- Add logging for structure name debugging"
git push origin main
```

## Результат

После этих изменений команда "build big house" будет:
1. Правильно распознаваться AI как "big-house"
2. Автоматически нормализоваться если AI вернет "big house"
3. Загружать NBT шаблон вместо процедурной генерации
4. Строить детализированный двухэтажный дом

---

**Статус:** ✅ Исправлено
**Дата:** 14 ноября 2024
**Версия:** 1.1.1
